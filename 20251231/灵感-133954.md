# 灵感笔记 - {2025-12-31}

## 💡 创意点

## ⚠️ 遇到的问题
1.需要通过公司电脑借助手机共享的网络上网，并通过手机安装的tailscale 进行网络穿透，连接家里的mac
    1.存在的问题是，由于tailscale、clash不能同时使用
    2.
    3.
2.需要通过代理连接mac上的vscode server,但ssh连接默认不走代理
    1.ssh -o "ProxyCommand=ncat --proxy 10.175.86.73:1080 --proxy-type socks5 %h %p" green@100.104.111.108

```mermaid
graph TB
    A["VS Code（本地客户端 / UI）"]
    B["扩展（UI 侧）"]
    C["文件同步与命令执行"]
    D["VS Code Server（远程主机）"]
    E["远程文件系统"]
    F["扩展（工作区侧）"]

    A -->|"用户操作：编辑、调试等"| C
    B -.->|"UI 侧扩展（如主题、GitLens 界面）"| A
    C -->|"SSH 隧道（加密 TCP 连接）"| D
    D -->|"处理：智能提示、调试、终端等"| E
    D -->|"执行：命令、脚本、构建等"| F
    F -.->|"工作区侧扩展（如语言服务器、调试器）"| D

    style A fill:#ffe0b2,stroke:#e65100
    style D fill:#c8e6c9,stroke:#388e3c

```

## ✅ 解决方案

## 📚 学到的知识

